[paths]
# Root directory for generated opto schema files.
# The GUI will save to: <save_root>/<animal_id>/<project_name>/schema.yaml
save_root = ./data

# Root directory for externally requested schemas.
# prep_patterns will resolve:
#   <schema_root>/<animal_id>/<schema_name>/schema.yaml
schema_root = \\ar-lab-nas1\DataServer\opto_schemas
